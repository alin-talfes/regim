# Only the publishable/public client values belong in a browser build.
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
# This repository currently pins the REGIM project's public values in src/lib/config.ts.
# Never add service_role, secret keys or VAPID private keys here.
